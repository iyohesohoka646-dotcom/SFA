# Semantic Flow Architecture（SFA）项目愿景

**文档类型**：项目愿景与总体思想  
**目标读者**：智能体（AI 执行者）  
**语言**：中文

---

## 1. 核心问题定义

当前主流 AI 辅助编程依赖**对话式交互**，形成“黑箱甲乙方模式”：

- 用户用自然语言描述需求  
- AI 生成代码文本（或 diff）  
- 用户无法直觉理解代码行为，必须反复要求 AI 解释或修改  
- 整个过程中，用户作为系统设计者的认知负担不降反升，从架构制定者退化为代码校对员  

该模式的根源在于**系统结构存在于人脑，而不存在于工具中**。AI 只操作文本，不维护模块边界、数据流向等结构信息。每次交互，用户都需从代码文本中重建结构，导致效率低下。

## 2. 现有范式局限：控制流代理

LangGraph、AutoGen、CrewAI 等框架将 AI 调用抽象为**控制流图**：

- 图节点：Agent 或 LLM 调用  
- 图边：跳转逻辑，通常由 LLM 在运行时决策  

这类框架加重了结构缺失问题：

- 运行时反复调用 LLM 决定下一步，进一步叠加序列化延迟  
- 修改某一模块时，AI 可能读取整个系统上下文，上下文窗口过大  
- 用户无法掌握 AI 的决策轨迹，调试只能依赖“思维链”文本分析  

## 3. SFA 的根本理念

SFA 提出从**控制流**到**数据流**的范式翻转：

- **拓扑先于代码**：数据流管道在设计阶段即由架构师固定，运行时不再依赖 AI 做路由决策  
- **AI 角色限定为纯执行者**：AI 仅负责在模块内部生成实现代码，绝不能决定数据流向、模块边界或系统结构  
- **结构显式化、持久化**：系统结构（模块、接口、管道）以机器可读形式存于 `.sfa/` 元数据中，不再隐含在对话或代码文本里  

根本原则：**工程师作为架构师，掌控蓝图（数据流图）；AI 作为代码手，填充实现。审查的对象从代码行转为模块两端的数据摘要。**

## 4. 思维模型：电路设计类比

SFA 将软件系统视为**信号处理电路**：

- **模块** = 集成电路（IC），对外暴露输入/输出引脚（接口契约），内部实现封装  
- **管道** = 电路板布线，连接一个模块的输出到另一个模块的输入  
- **契约** = 数据手册，定义信号格式与语义  
- **快照** = 示波器探头，在模块边界无损抓取数据波形  
- **探针** = 条件路由或断言，不影响主数据通路  

关键特性：**模块是可递归的层级结构**。一个模块内部可包含子模块与子管道，对外聚合为单一接口。支持封装（group）、拆封（ungroup）、进入（drill）等操作。工程师可在不拆开封装的条件下透视内部关键节点的摘要信号。

## 5. 核心机制

SFA 区别于现有框架的三大机制：

1. **契约即边界（Contract as Wall）**  
   模块间不共享内存，只通过管道传递数据。模块 A 的输出必须严格符合模块 B 的 Input Schema，否则运行前即触发阻断，禁止 AI “自动适配”。防止隐性意图蔓延。

2. **节点内聚的可视化快照（Snapshot Observability）**  
   每个模块在每次运行后自动缓存输入/输出摘要及元数据。架构师通过观察数据流图上的摘要信号，快速定位异常模块，无需 AI 重新梳理逻辑。

3. **静态语义占位（Static Semantic Placeholder）**  
   模块的接口签名和数据流向由用户预先定义（通过 DSL 或可视化界面，无需 AI）。AI 仅被调用来填充占位符内部的实现代码，类似定义好函数签名后生成函数体。架构在生成之前已固化。

## 6. 目标工作流（从用户视角）

1. **定义拓扑**：用户通过命令行或界面创建模块节点、定义输入/输出契约、连接管道。拓扑以声明式 DSL 存储。  
2. **生成实现**：针对每个模块，AI 在严格契约和可见性约束下生成代码，以 diff 形式提交人类审批。  
3. **运行与观测**：系统按拓扑执行模块，自动在边界记录快照。用户通过摘要视图查看数据流动结果，发现问题直接定位到具体模块。  
4. **迭代修改**：修改请求精确路由至目标模块，AI 仅基于该模块的契约和上游样例进行局部重构，不污染其他模块。

## 7. 终极目标

建立一种**以结构为中心、AI 为工具**的编程范式，使得：

- 软件系统的架构是显式的、可操作的数据流图  
- 工程师始终是架构师，不必陷入代码文本细节  
- 模块化从“文件组织原则”提升为“运行时可见的边界”  
- 调试从“通读代码 + AI 解释”转变为“查看数据摘要 + 探针检查”  

SFA 不是对话式编程的改良，而是从根本上将人与 AI 的关系重设为：**人定义数据流动的网络，AI 填充网络中的计算单元。**